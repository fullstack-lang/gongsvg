// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { GraphicalElementAPI } from './graphicalelement-api';
import { GraphicalElementDB } from './graphicalelement-db';

@Injectable({
  providedIn: 'root'
})
export class GraphicalElementService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  GraphicalElementServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private graphicalelementsUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gongsvg/go/v1/graphicalelements';

  constructor(
    private http: HttpClient
  ) { }

  /** GET graphicalelements from the server */
  getGraphicalElements(): Observable<GraphicalElementDB[]> {
    return this.http.get<GraphicalElementDB[]>(this.graphicalelementsUrl)
      .pipe(
        tap(_ => this.log('fetched graphicalelements')),
        catchError(this.handleError<GraphicalElementDB[]>('getGraphicalElements', []))
      );
  }

  /** GET graphicalelement by id. Will 404 if id not found */
  getGraphicalElement(id: number): Observable<GraphicalElementDB> {
    const url = `${this.graphicalelementsUrl}/${id}`;
    return this.http.get<GraphicalElementDB>(url).pipe(
      tap(_ => this.log(`fetched graphicalelement id=${id}`)),
      catchError(this.handleError<GraphicalElementDB>(`getGraphicalElement id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new graphicalelement to the server */
  postGraphicalElement(graphicalelementdb: GraphicalElementDB): Observable<GraphicalElementDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

		return this.http.post<GraphicalElementDB>(this.graphicalelementsUrl, graphicalelementdb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
				this.log(`posted graphicalelementdb id=${graphicalelementdb.ID}`)
			}),
			catchError(this.handleError<GraphicalElementDB>('postGraphicalElement'))
		);
  }

  /** DELETE: delete the graphicalelementdb from the server */
  deleteGraphicalElement(graphicalelementdb: GraphicalElementDB | number): Observable<GraphicalElementDB> {
    const id = typeof graphicalelementdb === 'number' ? graphicalelementdb : graphicalelementdb.ID;
    const url = `${this.graphicalelementsUrl}/${id}`;

    return this.http.delete<GraphicalElementDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted graphicalelementdb id=${id}`)),
      catchError(this.handleError<GraphicalElementDB>('deleteGraphicalElement'))
    );
  }

  /** PUT: update the graphicalelementdb on the server */
  updateGraphicalElement(graphicalelementdb: GraphicalElementDB): Observable<GraphicalElementDB> {
    const id = typeof graphicalelementdb === 'number' ? graphicalelementdb : graphicalelementdb.ID;
    const url = `${this.graphicalelementsUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put(url, graphicalelementdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated graphicalelementdb id=${graphicalelementdb.ID}`)
      }),
      catchError(this.handleError<GraphicalElementDB>('updateGraphicalElement'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
